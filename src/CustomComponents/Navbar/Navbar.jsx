import React, { Component } from "react";
import './Navbar.scss';

class Navbar extends Component {
  render() {
    return(
      <nav className="cog-chat-navbar">
        <h4 className="cog-chat-navbar-title">Cogniwide</h4>
      </nav>
    );
  }
}

export default Navbar;
